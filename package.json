{
  "name": "si-offline",
  "version": "0.6.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node scripts/build-dist.mjs",
    "build:fixture": "node scripts/build-fixture-job.mjs",
    "preview": "vite preview",
    "test:roundtrip": "node scripts/roundtrip-sec.mjs",
    "test:qc": "node scripts/qc-fixture-check.mjs",
    "test": "npm run test:roundtrip && npm run test:qc && npm run test:brackets && npm run test:annotations",
    "test:brackets": "node scripts/test-brackets.mjs",
    "test:annotations": "node scripts/test-annotations.mjs"
  },
  "devDependencies": {
    "vite": "^8.3.0"
  },
  "description": "Offline SpecsIntact alternative: .sec editor, review annotations (sidecar), QC-gated export, masters library, change log, tag-aware find, outline status. No CMS."
}
