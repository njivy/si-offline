{
  "name": "si-offline",
  "version": "0.4.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node scripts/build-dist.mjs",
    "build:fixture": "node scripts/build-fixture-job.mjs",
    "preview": "vite preview",
    "test:roundtrip": "node scripts/roundtrip-sec.mjs",
    "test:qc": "node scripts/qc-fixture-check.mjs",
    "test": "npm run test:roundtrip && npm run test:qc && npm run test:brackets",
    "test:brackets": "node scripts/test-brackets.mjs"
  },
  "devDependencies": {
    "vite": "^8.3.0"
  },
  "description": "Offline SpecsIntact alternative: .sec editor, guided bracket picker with Job-wide batch, lineage, journal, QC. No CMS."
}
